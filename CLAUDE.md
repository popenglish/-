# Pop English 教室サポート プロジェクト

このプロジェクトは、英語教室「Pop English」（pop.english.komaki@gmail.com）の先生の作業を手伝うためのもの。
**このファイルの指示は、どのモデル・どのセッションでも必ず守ること。** 迷ったら推測せず、ここと `docs/` の記録を確認する。

## ユーザーについて（最重要）

- 日本語話者。**返事は必ず日本語**で、絵文字を交えたやさしい口調で
- パソコンの専門知識は少ない。**専門用語を使わない**（使う場合は必ずやさしい言い換えを添える）
- Mac + Google Workspace ユーザー（スプレッドシート、ドキュメント、フォーム）
- **1メッセージ＝1〜2ステップまで。**「できた」の返事を待ってから次の手順へ進む。一度に長い手順を出すと迷子になる
- 音声入力なのか、短い・誤変換のあるメッセージが来ることがある（例：「セクション」＝セッション）。分からなければ候補を挙げて確認する

## 絶対ルール（過去の事故から決まったもの。破ると壊れる）

1. **タブ名に算用数字（1, 2, １, ２）を使わない。漢数字を使う**（中一A、中二）。全角・半角の見分けがつかず数式が壊れた事故があった
2. **数式に入れるタブ名は、ユーザーに実際のタブ名をコピーして貼ってもらう。**自分の推測で書かない
3. **通知表の数式は「列番号で直接取る」形を使う**（`VLOOKUP(...,6,FALSE)`）。項目名で探すMATCH式は1文字違いで壊れるので新規には使わない
4. **◎○△への変換数式は、点数タブに貼らない**（循環参照 #REF! になる）。入力タブに貼る
5. 貼り付けの案内では必ず：「セルを**1回クリック**してから ⌘V」（ダブルクリック禁止）
6. 横並びデータはカンマ区切り1行＋「データ → テキストを列に分割」で渡す。Markdownの表はスプレッドシートに貼れない
7. 検索と置換は必ず「**このシートのみ**」（全シート置換で通知表が壊れた事故があった）
8. 数式・ID・タブ名は**1文字も変えずそのまま**使う。要約・整形しない
9. Google Drive の `create_file` ツールはこの環境では**使えない**（承認が通らない）。成果物は**コピペできるテキスト**で渡す。`read_file_content`・`copy_file` は使える

## 何をしているプロジェクトか

紙のテスト → 成績を半自動で通知表にする仕組みを運用中：

```
点数タブ（正解数を手入力）→ 入力タブ（◎○△に自動変換）→ 通知表タブ（名前を選ぶと表示）→ 全員分PDF → 印刷
```

- 正確な仕様・数式・ID・各クラスの進み具合 → **`docs/成績表システム.md` を必ず読む**
- 最新の作業状態 → **`handoffs/` の一番新しいファイルを読む**

## スキル（作業の手順書）

| スキル | 使いどき |
|---|---|
| `/new-class` | 新しいクラスの成績表セットを作る |
| `/test-items` | テストから通知表の評価項目・説明文・満点表を作る |
| `/add-scores` | 既存クラスに点数タブを付けて、紙テストの結果を◎○△に自動反映する |
| `/sheet-doctor` | 数式が動かない・#REF!・空白・FALSE などのトラブル対応 |
| `/print-pdf` | 全員分PDF作成スクリプトの設定と案内 |
| `/handoff` | 作業の区切りに引き継ぎ書を保存 |

ユーザーがスキル名を知らなくても、**当てはまる作業なら該当スキルの手順に従うこと。**

## 品質チェック（返事を送る前に確認）

- [ ] 日本語で、専門用語なしで書いたか
- [ ] 手順は1〜2ステップに収まっているか
- [ ] 数式やタブ名は記録（docs/）と一致しているか（記憶で書いていないか）
- [ ] 配点・満点を出したときは合計を検算して見せたか
- [ ] コピペ素材は「縦ブロック」か「カンマ1行」の貼れる形になっているか

## 作業の記録

- 大きな作業が終わったら `docs/成績表システム.md` の該当箇所（クラスの進み具合など）を更新してコミット・プッシュする
- セッションの区切りには `/handoff` で引き継ぎ書を残す
- **この仕組みの目的：モデルやセッションが変わっても、記録を読めば同じ品質で続きができること**
